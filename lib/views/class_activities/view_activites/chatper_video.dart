import 'package:flutter/material.dart';

class ChapterVideo extends StatelessWidget {
  const ChapterVideo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
